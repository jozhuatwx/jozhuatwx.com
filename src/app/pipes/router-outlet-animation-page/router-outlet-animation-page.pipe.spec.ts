import { RouterOutletAnimationPagePipe } from './router-outlet-animation-page.pipe';

describe('RouterOutletAnimationPagePipe', () => {
  it('create an instance', () => {
    const pipe = new RouterOutletAnimationPagePipe();
    expect(pipe).toBeTruthy();
  });
});
